# SITETREE.md — ImageSmith

> Generated by tree-man after BRAIN.md. Maintained going forward by repo-maintainer's "update repo" trigger — do not hand-edit without updating both halves below.

## Route Tree

```
/                          (public)
```

## Route Table

| Path | Type | Auth | Purpose | Parent | Status |
|---|---|---|---|---|---|
| / | page | public | Single-page app: hero, converter tool, about section, footer with links | — | planned |

## Field Reference

- **Auth levels in use:** `public` only — no authentication in V1
- **Status progression:** `planned` → `built` → `verified`
- **API routes are in PLANNER.md** — `POST /api/reviews` and `GET /api/reviews` live there, not here

## Notes

- This is intentionally a 1-route app. All content lives on `/` — converter, about, footer.
- Reviews are submitted and displayed inline on the same page via API calls.
- V2 will add a `/cli` docs route and possibly a `/reviews` standalone page — out of scope for V1.
